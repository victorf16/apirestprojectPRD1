# Change these values to the ones used to create the App Service.
$RESOURCE_GROUP_NAME='<Reource-Group-Name>'
$APP_SERVICE_NAME='<App-Service-Name>'

az webapp deploy --name $APP_SERVICE_NAME --resource-group $RESOURCE_GROUP_NAME --src-path "<PATH>"